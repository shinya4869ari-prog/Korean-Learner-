# K-Learner（韓国語ニュース読解・歌詞対訳ナビ）開発ロードマップ ＆ やることリスト (TODO)

ユーザー様と Antigravity（AI）が次に進めるべきタスク、改善項目、および今後実装すると学習効率が向上する機能拡張を優先度順に体系化した「次やることリスト」です。

## 📌 今日の進捗 (2026-09-22)

- [x] **辞書データをCloudflare D1に移行（一部完了・一時停止）**
  - **完了**: Cloudflare D1データベースの作成とdictionaryテーブルの定義
  - **完了**: KRDICT（韓国語-日本語辞書）49,834件のデータをインポート
  - **完了**: Cloudflare Workersで辞書検索APIエンドポイントを作成
  - **完了**: WorkerのD1接続設定調整（workers_dev: falseでリモートD1を使用）
  - **課題**: Cloudflare Workersのデプロイメント問題でAPIが正常に動作せず
  - **一時対応**: index.htmlをBUILTIN_DICT（内蔵辞書）のみ使用

---

## 🔧 Cloudflare Workersの問題リスト（他のAI対応用）

### 問題概要

Cloudflare Workersで辞書検索APIエンドポイントを構築中ですが、デプロイメント後にAPIが正常に動作しません。

### 具体的な問題

1. **APIレスポンス**: `curl`でテストすると `{"error":"Word not found"}` が返される
2. **データベース接続**: D1リモートデータベースには49,834件のデータが存在（確認済み）
3. **デプロイメント**: `npx wrangler deploy` は成功するが、"No targets deployed" という警告が出る
4. **workers_dev設定**: `workers_dev: true` では動作するが、`workers_dev: false` では動作しない

### 試した設定

- `workers_dev: false` - リモートD1を使用設定
- `workers_dev: true` - ローカル開発環境設定
- routes設定の追加・削除
- worker.jsにデバッグログ追加

### 現在の設定ファイル

- **wrangler.jsonc**: workers_dev: false設定
- **worker.js**: 辞書検索APIエンドポイント実装済み
- **D1データベース**: korean-learner-dict (ID: 98ff31cb-043e-436d-ab4e-a1bf45cc0954)

### 期待する動作

`https://koreanews.seronworks.dev/api/dictionary?word=원` で正しいJSONレスポンスが返されること

---

## 🔧 修正版インポートスクリプトの設計（term*bank*\*.json データ修正用）

### 背景

D1の辞書データ（dictionaryテーブル）に日本語訳ではなく韓国語の読み方（見出し語）が入っている問題を修正する必要がある。

### データ構造（Yomitan形式）

```json
["置換되다","치환되다","動詞","v",0,[{"type":"structured-content","content":[...日本語訳データ...]}]
```

**配列のインデックス**:

- 0: 置換되다（韓国語）
- 1: 치환되다（ハングル）
- 2: 動詞（品詞）
- 3: v（活用型）
- 4: 0（レベル）
- 5: [...日本語訳データ...]（これをmeaningに入れるべき）

### 処理フロー

1. `dict_data/term_bank_1.json` 〜 `term_bank_11.json` を順に読み込む
2. 各配列の5番目の要素（構造化されたJSON）からテキストを抽出
3. 日本語部分（`lang: "ja"`）のみを抽出してクリーンアップ
4. D1のINSERT文を作成（word, hanja, pos, meaning, level）

### 抽出ロジック

- 5番目の要素はstructured-content形式
- `content` 配列内の `lang: "ja"` の要素を抽出
- テキストのみを抽出（HTMLタグなどを削除）

### 注意事項

- 書き込み制限は日本時間の朝9:00（UTC 0:00）にリセットされる
- それまでD1への書き込み処理やインポートスクリプトは絶対に実行しない

---

## ⚡ 【最優先】直近の対応タスク (Phase 2: 残課題・操作性の完全ブラッシュアップ)

- [x] **1. 楽曲の削除機能の完全修正**
  - **現状の課題**: チップのゴミ箱アイコン（🗑️）を押しても削除されない／歌詞モードなのにニュース記事が表示されるなどの不整合がある。
  - **対応内容**:
    1. チップのゴミ箱（🗑️）を押した瞬間に、画面の選択肢・リストから即座にフェードアウト消去。
    2. Supabase（`tracks` テーブル）から該当レコードを確実に `DELETE` する。
    3. 削除完了トーストの表示と、次回読み込み時にも確実に復活しないよう除外リスト（ローカルストレージおよびDB）へ保存。
- [x] **2. 画面下の再生バー（下部プレイヤー）の完全撤去 & 余白スリム化**
  - **現状の課題**: 画面最下部の再生バー（帯）が不要で、閉じても再度表示される場合がある。また `body` に余計な `padding-bottom: 95px` が残っている。
  - **対応内容**:
    1. `#audio-toolbar` および関連する下部固定プレイヤーのコード・DOMを画面から完全撤去。
    2. 画面下の余計な余白（`padding-bottom`）をなくし、画面全体を広く使えるようにする。

---

## 🚀 今後の機能拡張ロードマップ (Phase 3: さらなる学習効率化・機能拡張)

- [ ] **1. 単語帳のスマート復習機能（Anki風 間隔反復）**
  - 保存した単語を「覚えた」「まだ」「苦手」でスワイプ分類できるフラッシュカード機能
  - 歌詞やニュースの例文と一緒にワンタップで音声再生
- [ ] **2. 音声読み上げ（TTS）の速度・ピッチ微調整バー**
  - ネイティブ速度（1.0x）だけでなく、初心者向けのゆっくり再生（0.7x〜0.85x）をワンタップで切り替え
- [ ] **3. オフライン対応強化（Service Worker キャッシュ最適化）**
  - 一度閲覧したニュースやお気に入りの曲を完全オフラインでも閲覧・学習可能にするオフラインキャッシュ
- [ ] **4. ニュースカテゴリのパーソナライズ & キーワードフィルター**
  - 興味のある分野（エンタメ、IT・科学、経済、文化）に絞り込んで学習効率を最大化
- [ ] **5. 辞書データの定期的な更新**
  - KRDICTの最新データを定期的に取得してCloudflare D1に更新
  - 新しい単語や意味の追加、誤った翻訳の修正
- [ ] **6. 辞書検索の高速化**
  - キャッシュ機能の強化で頻繁に検索する単語の応答を高速化
  - 部分一致検索やあいまい検索の実装
- [ ] **7. Cloudflare Workersのエラーハンドリング強化**
  - APIエラー時のフォールバック処理
  - レート制限対策と再試行ロジック
