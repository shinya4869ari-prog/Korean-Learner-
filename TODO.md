# K-Learner やることリスト (TODO)

> **現在のステータス**: v1.2.5（Cloudflare本番デプロイ済み / PWA対応）

---

## 🎯 次にやること（未完了・やりたいことリスト）

### ⚡ 優先度【高】：直近で取り組むタスク

- [ ] **1. 【自律進化辞書】n8n × Cloudflare D1 文節・活用形ペアの自動蓄積**
  - **やりたいこと**: ニュースを読み込むたびに、AIが文節と訳のペア（`먹었습니다`＝`食べました` 等）を自動抽出し、D1の辞書に蓄積して「ホバー時に100%光る辞書」へ自然進化させる。
  - **進める手順**:
    - [ ] **Step 1: Workerに辞書一括追加API（`POST /api/dictionary/batch`）の実装**
      - n8nから送られてきた単語・文節リストを `INSERT OR IGNORE` で安全にD1へ保存する受け口を作成（APIキーガード付き）。
    - [ ] **Step 2: n8nのLLMプロンプト作成（文節・活用形ペアの抽出）**
      - ニュース本文と訳から「本文の形」「原形」「品詞」「日本語訳」「漢字」をJSON配列で出力させるプロンプトを作成。
    - [ ] **Step 3: n8nワークフローへの組み込み（HTTP Requestノード）**
      - AIが作った文節ペアを、Step 1 のAPIへPOSTしてD1を自動更新。
    - [ ] **Step 4: アプリ側の動作確認**
      - 本文の活用形をホバーした際、新しくD1に蓄積された単語と連動して緑色にピカッと光るかテスト確認。

- [ ] **2. 「🤖 AI再判定」結果の自動蓄積（Supabase / D1へのUpsert）**
  - **現状**: AI再判定で正しい原形や意味が出ても、その場限りの表示で次回タップ時には再判定が必要になる。
  - **やりたいこと**:
    - AIが解析した結果をバックグラウンドで自動的に辞書データへ保存し、次回からは即座に高精度な訳が出るようにする。

- [ ] **3. 音声読み上げ（TTS）の速度微調整（0.75x 〜 1.0x）**
  - **やりたいこと**:
    - ニュース本文や単語の発音を「ゆっくり聴く（0.8x）」と「通常速度（1.0x）」で切り替えられるようにする。

---

### 💡 優先度【中】：今後の学習機能拡張

- [ ] **3. 単語帳のスマート復習機能（フラッシュカード / Anki風）**
  - 保存した単語を「覚えた」「苦手」でスワイプ分類できる暗記カード機能。
- [ ] **4. D1辞書データの日本語訳適正化（term_bank精査）**
  - D1データベースの辞書データ内に一部ハングル混ざりがある箇所のデータクリーンアップ（※Cloudflare無料枠の書き込みリセット後に順次実施）。
- [ ] **5. ニュースカテゴリのパーソナライズ & キーワードフィルター**
  - 興味のある分野（社会・エンタメ・ITなど）に絞り込んで効率的に読解学習できる機能。

---

## 💡 検討中の提案・アイデア（RFC）

### 提案1：n8n × D1 辞書の自律進化システム（文節ペア自動追加）
> **コンセプト**: ニュースを読み込むたびに、AIが文節と訳のペア（`먹었습니다`＝`食べました` 等）を自動抽出し、Cloudflare D1 の辞書に蓄積して辞書を自然に太らせる仕組み。

- **作業手順**:
  1. **Workerに辞書登録API追加**: `POST /api/dictionary/batch` を実装し、`INSERT OR IGNORE` で新規の活用形・文節のみ安全に一括追加できるようにする。
  2. **n8nプロンプト設計**: LLMノードでニュースから「本文の形」「原形」「品詞」「日本語訳」の4点をJSON配列で出力させる。
  3. **n8nに登録ノード追加**: HTTP Request ノードで Worker の辞書追加APIへ送信。
  4. **アプリ側の動作確認**: ニュース内の文節をホバーした際、新しく追加された辞書データと連動して緑色にピカッと光るかテスト確認。
- **ポイント**:
  - 文法学習アプリとしての品質を落とさないため、AIには「原形」「品詞」もセットで返させる。
  - すでに登録済みの単語はスキップしてデータベースの書き込み回数を抑える。

---

## ✅ 完了したこと（実装済み機能一覧）

### 📱 直近のアップデート (v1.2.0 〜 v1.2.3)

- [x] **ニュース配信日（日付）が過去日付固定（2026.08.21）になっていた不具合の解消（v1.2.5）**
  - Supabaseから取得したニュースに `date` プロパティが未設定だったため、固定値のフォールバックが表示されていた問題を修正。
  - Supabaseの日時データ（`published_at` / `created_at`）から日本時間の `YYYY.MM.DD` 形式を自動生成・表示するように改修。
- [x] **「単語帳に保存」モーダルの初期表示バグ＆閉じられない不具合の解消（v1.2.4）**
  - CSSの `display: flex !important` が原因で起動時に強制表示され、閉じるボタンが効かなくなっていた問題を修正。
  - 初期状態を `display: none` に設定し、`active` クラスによる確実な開閉制御へ変更。
- [x] **「★ 単語帳に保存」時の確認・編集モーダル（v1.2.3）**
  - 保存ボタンを押した際に画面中央へリッチなモーダルがポップアップ。
  - 見出し語（原形）・漢字表記・品詞（プルダウン）・重要度（★1〜★3）・意味をその場で微調整・追記してからSupabaseおよび単語帳に確定保存。
  - マイ単語帳カードにも「✏️ 編集」ボタンを追加し、登録済み単語もいつでも修正可能に。
- [x] **単語辞書カードの段落直前インラインスライド展開**
  - ニュース本文中の単語をタップした際、画面最上部ではなく**タップした段落カードの直前（すぐ上／段落の間）に紫枠の辞書カードがスムーズにスライドイン**。
  - 辞書未登録語や特殊な活用形でも、カードが消えることなく「🤖 AIで解析」ボタンを即座に表示。
- [x] **漢字表記（hanja）のハングル混ざり自動除去**
  - `侵入하다` などのハングル混ざり漢字からハングルを自動トリムし、純粋な漢字（`侵入`）のみを美しく紫バッジ表示。
- [x] **助詞分離 & 活用形の原形復元エンジン（Lemmatization）**
  - `징역형이나` ➔ `징역형`（助詞「이나」「나」「한테」を自動分離）。
  - `훔쳐` ➔ `훔치다`、`밝혔다` ➔ `밝히다`、`담아` ➔ `담다`、`달아난` ➔ `달아나다` 等の連用形・過去形・連体形を原形へ自動逆算復元。
  - 「도주」「인근」などの名詞に勝手に「다」が付く捏造を防止する名詞ガードを適用。
- [x] **品詞ラベルの日本語化 ＆ 出所（Origin）4色バッジ**
  - `[noun]` ➔ `[名詞]`、`[verb]` ➔ `[動詞]` 等の完全日本語表示。
  - データの信頼性を一目で判別できる4色バッジ（`[My単語帳]`, `[記事解析]`, `[D1辞書]`, `[AI推測]`）を表示。
- [x] **PWA更新通知 & Service WorkerのHTML完全生通信化**
  - Service Worker (v9) でHTMLナビゲーションを完全生通信（Network Only）にし、キャッシュによる反映遅延を解消。
  - 新バージョン更新時に画面右上に「🔄 最新版に更新 (v1.2.2)」バナーを表示。
- [x] **画面下の再生バー（下部プレイヤー）の完全撤去 & 余白スリム化**
- [x] **楽曲チップのゴミ箱削除機能の完全修正（Supabase連動）**
- [x] **歌詞ライブラリのアーティスト別2階層管理システム（Spotify/ライブラリ風）**
  - 登録曲が増加しても快適に探せるよう、「🎤 アーティスト一覧（Tier 1）」➔「各アーティストの楽曲一覧（Tier 2）」の2階層ナビゲーションを実装。
  - 「김나영（Kim Na Young）」など複数楽曲・共演曲（with 이승환, with 버블 시스터즈等）が存在する場合も主アーティストごとに美しく自動集約。
  - 現在再生中アーティスト・楽曲の「▶ 再生中」バッジ表示、リアルタイム検索フィルター、および「🎵 全曲一覧」へのワンタップ切替に対応。
- [x] **歌詞ヘッダーへのアーティスト名明記 ＆ YouTubeリンクボタン（Supabase連動）追加**
  - 歌詞表示ヘッダーに「🎤 アーティスト名（DAWN 等）」をタグおよび見出し直上にしっかりと明示。
  - Supabaseのtracksテーブルから「youtube_id」「mv_url」を取得し、公式MV・音源を直接開く「🔴 YouTubeで見る ↗」ボタンをSpotifyボタンの隣に追加。未登録曲でも自動検索クエリで快適にYouTube動画を開けるフォールバックを完備。
- [x] **楽曲ヘッダーの重複整理（日本語タイトルのインライン化・単一アーティスト表示・自動翻訳ボタン完全撤去）**
  - 日本語訳が存在する場合はタイトルのすぐ横に自然に添え、原題と同一の曲では重複表示を排除。
  - アーティスト名を曲名直下の1箇所に集約し、上のタグ列との二重表示を解消。不要な自動翻訳ボタンおよび自動保存処理を完全に撤去。
