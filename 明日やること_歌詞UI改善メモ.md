# 明日やることメモ（韓国語学習アプリ 改善タスク）

## 1. 楽曲の削除機能の完全修正
- **現状の課題**: チップのゴミ箱アイコン（🗑️）を押しても削除されない／歌詞モードなのにニュース記事が表示されるなどの不整合がある。
- **対応内容**:
  1. チップのゴミ箱（🗑️）を押した瞬間に、画面リストから即座に消去。
  2. Supabase（`tracks` テーブル）から該当レコードを確実に `DELETE` する。
  3. 削除完了トーストの表示と、次回読み込み時にも確実に復活しないよう除外リストへ保存。

---

## 2. 画面下の再生バー（プレイヤー）の完全撤去
- **現状の課題**: 画面最下部の再生バー（帯）が邪魔で、閉じても再度表示されるなど消えない。
- **対応内容**:
  1. `#audio-toolbar` および関連する下部固定プレイヤーのコード・DOMを画面から完全撤去。
  2. 画面下の余計な余白（`padding-bottom`）をなくし、画面全体を広く使えるようにする。

---

## 3. 歌詞画面の超シンプル化（スクロール激減・省スペース化） 【✅ 対応完了】
- **対応内容（Spotify風のスリム歌詞ビューへ刷新）**:
  1. **行ごとのボタン類を全廃**: 各行の「再生ボタン」「一時停止」「停止」「1回だけ」「コピー」「フレーズ保存」などのボタンバーを全削除。
  2. **行番号（1番・2番等）の撤去**: 各行の「1」「2」丸バッジを削除し、左の無駄なインデント（余白）を解消。
  3. **コンパクトな行間**: 韓国語歌詞と日本語訳をギュッと詰め、1画面に多数の行がスッキリ収まるスリム歌詞カード（`.lyrics-line-card`）を適用（スクロール量を激減）。

---

## 4. 単語調べの「追従型スマート単語バー」化 【✅ 対応完了】
- **対応内容**:
  1. 歌詞の中の気になる単語をタップ／クリックした時だけ、画面上部に**1つだけ固定表示された辞書バー（追従スマート単語調べ）**に意味・発音・単語帳保存がスッと表示。
  2. スクロールしても辞書バーは常に画面上部（ヘッダー直下: `top: 72px` / スマホ `60px`）についてくるため、どの行を読んでいてもその場で単語の意味が確認可能。
  3. 各行に重複して30〜50個並んでいた不要な単語入力欄（`card-lookup-box`）を全廃し、スクロール量とDOMを大幅軽量化。
  4. 選択中の単語にネオンハイライトが付与され、閲覧中の単語が一目で把握可能に。

---

## 5. 新規アプリ（NASDAQ100×ゴールド シーソー運用管理）
- `n8n-prompts/prompts/seesaw-portfolio/構築手順書.md` にステップ1（Cloudflare Worker作成）からの手順を保存済み。
- 歌詞アプリの整理がついたら、こちらのステップ1から着手可能。
